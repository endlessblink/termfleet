# Worktree merge audit - 2026-07-07

Goal: collapse stale TermFleet worktrees into the active
`fix/canvas-perf-and-wrap-spill` branch without losing source work or applying
stale deletions.

## Committed branch audit

- `tc-033-tasklist`: no unique right-side commits relative to the active branch.
- `integration`: no unique right-side commits relative to the active branch.
- `tc-034-project-grouping`: no unique right-side commits relative to the active branch.
- `tc-release-hardening`: no unique right-side commits relative to the active branch.
- `feat-emoji-picker`: one stale unique commit (`8a912ee`) was inspected, but the
  active branch already contains the newer emoji picker implementation from
  `7f96237`. Applying `8a912ee` would remove the embedded project emoji behavior,
  so the cherry-pick was skipped rather than merged.

## Preserved uncommitted work

- `2026-07-07-termfleet-tasklist-uncommitted.patch` preserves the dirty
  `termfleet-tasklist` worktree changes. The `MagicCanvas.tsx` part prefers a
  high-confidence status-worker summary over durable/local task identity. That is
  intentionally not applied to the active branch because the current reliability
  direction keeps task identity bounded to structured/provenance-safe sources.
  The `vite.config.ts` part is an absolute-path worktree-only `node_modules`
  allowance and should not be promoted.
- `2026-07-07-release-build-e5f5576-uncommitted.patch` preserves the dirty temp
  release worktree dependency addition for `tauri-plugin-opener`.
- Untracked `node_modules` directories in worktrees were dependency artifacts and
  were not archived.
