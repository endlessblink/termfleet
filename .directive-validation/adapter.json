{
  "allowedPaths": ["MASTER_PLAN.md", "docs/", "scripts/", "src/", "tests/", ".directive-validation/"],
  "checks": [
    {"name":"build","command":"npm","args":["run","build"]},
    {"name":"map_terminal_scroll_regression","command":"npx","args":["playwright","test","tests/map-terminal-rendering.spec.ts","-g","map terminal wheel navigation does not force the viewport back to live output","--reporter=line"]},
    {"name":"installed_release","command":"npm","args":["run","verify:installed-release"]}
  ],
  "acceptance": {
    "runtimeIdentity": "dock-launched installed TermFleet release",
    "runtime": "dock-launched installed TermFleet release",
    "runtimeEvidence": {"check":"installed_release","independent":true,"proves":"the installed release identity is verified"},
    "userVisibleOutcomes": ["Map terminal wheel scrolling stays at the selected scrollback position", "Ordinary live output continues to follow the bottom"],
    "evidenceChecks": [
      {"check":"build","proves":"the current frontend builds"},
      {"check":"map_terminal_scroll_regression","proves":"wheel navigation preserves the user viewport and live output still follows normally"},
      {"check":"installed_release","proves":"the dock-launched installed release is the runtime identity under test"}
    ],
    "checkToOutcome": [
      {"check":"build","outcome":"the frontend builds successfully"},
      {"check":"map_terminal_scroll_regression","outcome":"wheel scroll preserves the selected viewport and ordinary live output follows the bottom"},
      {"check":"installed_release","outcome":"the installed dock runtime identity is verified"}
    ],
    "evidenceMappings": [
      {"check":"build","outcome":"the frontend builds successfully"},
      {"check":"map_terminal_scroll_regression","outcome":"wheel scroll preserves the selected viewport and ordinary live output follows the bottom"},
      {"check":"installed_release","outcome":"the installed dock runtime identity is verified"}
    ],
    "requirementEvidence": [
      {"requirement":"Wheel scrolling on a map terminal reveals older output without being pulled back to the newest output","check":"map_terminal_scroll_regression","assertion":"map-scrollback-preserves-user-viewport","proof":"The focused wheel regression detects forced bottom-follow during wheel navigation.","independent":true,"proves":"the focused regression detects forced bottom-follow during wheel navigation"},
      {"requirement":"New output still follows the bottom when the user has not manually scrolled away","check":"map_terminal_scroll_regression","assertion":"map-live-output-follows-bottom","proof":"The focused regression preserves ordinary live-output following.","independent":true,"proves":"the focused regression preserves ordinary live-output following"},
      {"requirement":"The behavior is covered by a focused regression and the installed release remains buildable","check":"installed_release","assertion":"installed-runtime-identity","proof":"The installed release identity check passes for the build under test.","independent":true,"proves":"the installed release identity check passes for the build under test"}
    ]
  },
  "requiredChecks": ["build", "map_terminal_scroll_regression", "installed_release"],
  "checkTimeoutMs": 600000,
  "visualPaths": [],
  "visualRequiredChecks": [],
  "designGate": {"required": false},
  "taskBinding": {"required": true, "targets": ["map", "terminal", "scroll"]},
  "requiresUserApproval": true,
  "adapterApproval": {"required": true},
  "contractPath": ".directive-validation/contract.json",
  "integration": {"required": true, "protocol": "directive-agent-v1"},
  "coverage": {
    "required": true,
    "seeds": [
      {"id":"wheel-scrollback","description":"Scroll a map terminal into older output with the wheel"},
      {"id":"live-follow","description":"Leave a map terminal at the bottom while new output arrives"}
    ],
    "invariants": [
      {"id":"user-viewport-preserved","statement":"Wheel navigation does not force the viewport back to newest output","check":"map_terminal_scroll_regression"},
      {"id":"bottom-follow-preserved","statement":"Uninterrupted live output continues to follow the newest output","check":"map_terminal_scroll_regression"}
    ],
    "mutations": [
      {"id":"force-bottom-after-wheel","target":"Schedule bottom-follow after wheel navigation","expected":"fail","check":"map_terminal_scroll_regression","command":"node","args":["scripts/verify-map-scroll-mutation.mjs","force-bottom-after-wheel","map_terminal_scroll_regression","Schedule bottom-follow after wheel navigation"],"isolated":true,"failureSignal":"invariant failed"},
      {"id":"disable-live-follow","target":"Suppress bottom-follow for ordinary live output","expected":"fail","check":"map_terminal_scroll_regression","command":"node","args":["scripts/verify-map-scroll-mutation.mjs","disable-live-follow","map_terminal_scroll_regression","Suppress bottom-follow for ordinary live output"],"isolated":true,"failureSignal":"invariant failed"}
    ],
    "mappings": [
      {"seed":"wheel-scrollback","invariant":"user-viewport-preserved","mutation":"force-bottom-after-wheel"},
      {"seed":"live-follow","invariant":"bottom-follow-preserved","mutation":"disable-live-follow"}
    ]
  }
}
