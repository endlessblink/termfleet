# Project validation

TermFleet validation is fail-closed: explicit user closes must stay gone after restart, untouched terminals must return, and the installed dock-launched runtime must pass the configured checks. Technical validation can pass, but the final visible milestone still requires the user's approval.
